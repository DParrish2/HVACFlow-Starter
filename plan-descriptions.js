(() => {
  function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
  function formatDate(unix){if(!unix)return '';const d=new Date(Number(unix)*1000);return Number.isNaN(d.getTime())?'':d.toLocaleDateString();}
  function includedSeats(plan){return ({starter:1,professional:5,business:15})[plan]||0;}
  function planLabel(data){if(data?.plan==='business'&&Number(data?.extra_seats||0)>=1)return 'Business Plus';return ({starter:'Starter',professional:'Professional',business:'Business'})[data?.plan]||'Unknown';}
  function subscriptionSummary(data){
    const extra=Math.max(0,Number(data.extra_seats||0));
    const included=includedSeats(data.plan);
    const total=Math.max(0,Number(data.seat_limit||included+extra));
    const used=Math.max(0,Number(data.member_count||0));
    const available=Math.max(0,total-used);
    const status=data.test_account?'Test account':(data.cancel_at_period_end?'Cancels at period end':(data.status||'Active'));
    return `<div class="card" style="margin-top:16px"><h3 style="margin-top:0">Current subscription</h3><div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:12px"><div><div class="sub" style="margin:0 0 4px">Plan</div><div style="font-size:24px;font-weight:800">${esc(planLabel(data))}</div></div><div><div class="sub" style="margin:0 0 4px">Seats used</div><div style="font-size:24px;font-weight:800">${used} / ${total}</div><div style="font-size:13px;color:#667085">${available} available</div></div><div><div class="sub" style="margin:0 0 4px">Subscription status</div><div style="font-size:18px;font-weight:700;text-transform:capitalize">${esc(status)}</div></div><div><div class="sub" style="margin:0 0 4px">Included seats</div><div style="font-size:24px;font-weight:800">${included}</div></div><div><div class="sub" style="margin:0 0 4px">Paid add-on seats</div><div style="font-size:24px;font-weight:800">${extra}</div></div><div><div class="sub" style="margin:0 0 4px">Total seat capacity</div><div style="font-size:24px;font-weight:800">${total}</div></div><div><div class="sub" style="margin:0 0 4px">Seats available</div><div style="font-size:24px;font-weight:800">${available}</div></div><div><div class="sub" style="margin:0 0 4px">Seat add-on cost</div><div style="font-size:24px;font-weight:800">$${(extra*8).toFixed(2)}<span style="font-size:13px;font-weight:400"> / month</span></div></div></div></div>`;
  }
  async function refreshSubscriptionManagement(){
    const host=document.getElementById('subscriptionManagement');if(!host||typeof sb==='undefined')return;
    const {data:{session}}=await sb.auth.getSession();if(!session){host.innerHTML='';return;}
    try{
      const response=await fetch('/api/subscription',{headers:{Authorization:`Bearer ${session.access_token}`}});const data=await response.json();if(!response.ok)throw new Error(data.error||'Subscription status could not be loaded.');if(data.member_role&&data.member_role!=='owner'){host.innerHTML='';return;}
      if(data.active){try{const {data:teamRows,error:teamError}=await sb.rpc('hvacflow_team_summary');if(!teamError){const team=Array.isArray(teamRows)?teamRows[0]:teamRows;if(team){data.member_count=Number(team.member_count||0);data.seat_limit=Number(team.seat_limit||0);}}}catch(_e){}}
      if(!data.active){host.innerHTML='<div class="card" style="margin-top:16px"><h3>Current subscription</h3><p class="sub" style="margin:0">No active HVACFlow subscription was found.</p></div>';return;}
      const summary=subscriptionSummary(data);
      if(data.test_account){host.innerHTML=`${summary}<div class="card" style="margin-top:16px"><h3>Subscription management</h3><p class="sub" style="margin:0">Test accounts do not have a cancellable live subscription.</p></div>`;return;}
      const endDate=formatDate(data.current_period_end);
      if(data.cancel_at_period_end){host.innerHTML=`${summary}<div class="card" style="margin-top:16px"><h3>Subscription cancellation scheduled</h3><p>Your HVACFlow access remains active${endDate?` through <strong>${esc(endDate)}</strong>`:''}. After that date, the subscription will end and you will not be billed for another period.</p><button class="btn secondary" id="resumeSubscription">Keep My Subscription</button><div id="subscriptionManageMsg"></div></div>`;document.getElementById('resumeSubscription').onclick=()=>manageSubscription('resume');}
      else{host.innerHTML=`${summary}<div class="card" style="margin-top:16px"><h3>Manage subscription</h3><p class="sub">Need to stop your plan? You can cancel future renewal and keep access through the end of your current billing period.</p><button class="btn danger" id="cancelSubscription">Cancel Subscription</button><div id="subscriptionManageMsg"></div></div>`;document.getElementById('cancelSubscription').onclick=()=>manageSubscription('cancel');}
    }catch(error){host.innerHTML=`<div class="msg err">${esc(error.message)}</div>`;}
  }
  async function manageSubscription(action){
    const msg=document.getElementById('subscriptionManageMsg');const button=document.getElementById(action==='cancel'?'cancelSubscription':'resumeSubscription');
    if(action==='cancel'&&!confirm('Cancel your HVACFlow subscription at the end of the current billing period? You will keep access until then.'))return;if(action==='resume'&&!confirm('Keep your HVACFlow subscription active and continue future renewals?'))return;
    try{if(button){button.disabled=true;button.textContent=action==='cancel'?'Scheduling cancellation…':'Restoring subscription…';}const {data:{session}}=await sb.auth.getSession();if(!session)throw new Error('Please sign in again.');const response=await fetch('/api/manage-subscription',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.access_token}`},body:JSON.stringify({action})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Subscription could not be updated.');if(msg)msg.innerHTML=`<div class="msg ok">${action==='cancel'?'Cancellation scheduled. You will keep access until the end of your current billing period.':'Your subscription will continue renewing normally.'}</div>`;setTimeout(refreshSubscriptionManagement,500);}catch(error){if(msg)msg.innerHTML=`<div class="msg err">${esc(error.message)}</div>`;if(button){button.disabled=false;button.textContent=action==='cancel'?'Cancel Subscription':'Keep My Subscription';}}
  }
  async function startBusinessPlus(button){
    const msg=document.getElementById('checkoutMsg');if(msg)msg.innerHTML='';const original=button.textContent;button.disabled=true;button.textContent='Opening secure checkout…';
    try{const {data:{session}}=await sb.auth.getSession();if(!session)throw new Error('Please sign in again.');const response=await fetch('/api/business-plus-checkout',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.access_token}`},body:'{}'});const data=await response.json();if(!response.ok)throw new Error(data.error||'Checkout could not be started.');location.href=data.url;}catch(error){if(msg)msg.innerHTML=`<div class="msg err">${esc(error.message)}</div>`;button.disabled=false;button.textContent=original;}
  }
  function renderPlans(){
    const billing=document.getElementById('billing');const plans=billing?.querySelector('.plans');if(!plans)return;
    plans.dataset.planDescriptionsReady='1';plans.style.gridTemplateColumns='repeat(4,minmax(0,1fr))';
    plans.innerHTML=`<div class="card plan"><h2>Starter</h2><div class="price">$29.99 <small>/ month</small></div><p><strong>1 user included.</strong></p><p>Complete HVACFlow core platform.</p><p><strong>Extra seats:</strong> $8/user/month up to 4 total users.</p><button class="btn primary subscribe" data-plan="starter">Choose Starter</button></div><div class="card plan"><h2>Professional</h2><div class="price">$59.99 <small>/ month</small></div><p><strong>Up to 5 users included.</strong></p><p>Everything in Starter plus shared company access.</p><p><strong>Extra seats:</strong> $8/user/month up to 9 total users.</p><button class="btn primary subscribe" data-plan="professional">Choose Professional</button></div><div class="card plan"><h2>Business</h2><div class="price">$99.99 <small>/ month</small></div><p><strong>Up to 15 users included.</strong></p><p>Everything in Professional for larger teams.</p><p><strong>Extra seats:</strong> $8/user/month above 15.</p><button class="btn primary subscribe" data-plan="business">Choose Business</button></div><div class="card plan"><h2>Business Plus</h2><div class="price">$107.99 <small>/ month</small></div><p><strong>Business + 1 paid seat.</strong></p><p>Starts with 16 total seats and keeps the same $8/user/month add-on rate.</p><button class="btn primary business-plus-subscribe">Choose Business Plus</button></div>`;
    plans.querySelector('.business-plus-subscribe')?.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();startBusinessPlus(e.currentTarget);});
    if(billing&&!document.getElementById('subscriptionManagement')){const host=document.createElement('div');host.id='subscriptionManagement';billing.appendChild(host);setTimeout(refreshSubscriptionManagement,100);}
    if(!document.getElementById('planGridResponsive')){const style=document.createElement('style');style.id='planGridResponsive';style.textContent='@media(max-width:1100px){#billing .plans{grid-template-columns:repeat(2,1fr)!important}}@media(max-width:700px){#billing .plans{grid-template-columns:1fr!important}}';document.head.appendChild(style);}
  }
  function refreshDashboardAppointments(){const upcoming=document.getElementById('upcoming');if(!upcoming||typeof cache==='undefined')return;const appointments=Array.isArray(cache.Appointments)?cache.Appointments:[];const future=appointments.filter(a=>a.scheduled_for&&new Date(a.scheduled_for)>=new Date()&&a.status!=='cancelled').sort((a,b)=>new Date(a.scheduled_for)-new Date(b.scheduled_for));const card=upcoming.closest('.card');if(card){const heading=card.querySelector('h3');if(heading)heading.textContent='Appointments';card.style.cursor='pointer';const open=()=>document.querySelector('#nav button[data-page="appointments"]')?.click();card.onclick=open;card.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open();}};}const next=future[0];upcoming.innerHTML=`<div style="font-size:34px;font-weight:800;color:#0d47a1;line-height:1">${appointments.length}</div><div style="color:#667085;margin-top:5px">Total appointments</div>${next?`<div style="margin-top:12px"><strong>Next:</strong> ${esc(next.customer_name||'Customer')} · ${new Date(next.scheduled_for).toLocaleString()}</div>`:'<div style="margin-top:12px;color:#667085">No upcoming appointments.</div>'}<div style="margin-top:10px;color:#0d47a1;font-weight:700">View appointments →</div>`;}
  function installDashboardAppointmentFix(){if(window.__hvacflowDashboardAppointmentFix||typeof window.loadDashboard!=='function')return;window.__hvacflowDashboardAppointmentFix=true;const original=window.loadDashboard;window.loadDashboard=async function(...args){const result=await original.apply(this,args);refreshDashboardAppointments();return result;};try{loadDashboard=window.loadDashboard}catch(_e){}setTimeout(refreshDashboardAppointments,100);}
  function initialize(){renderPlans();installDashboardAppointmentFix();refreshDashboardAppointments();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',initialize,{once:true});else initialize();
  if(typeof sb!=='undefined')sb.auth.onAuthStateChange(()=>setTimeout(()=>{renderPlans();refreshSubscriptionManagement();refreshDashboardAppointments();},200));
})();
